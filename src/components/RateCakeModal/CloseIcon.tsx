import { IoClose } from "react-icons/io5";

const CloseIcon = () => (
    <span className="text-gray duration-300 hover:text-black">
        <IoClose size="32" />
    </span>
);

export default CloseIcon;
